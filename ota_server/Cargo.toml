[package]
name = "ota_server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["tokio"] }
tower-http = { version = "0.5", features = ["fs"] }
tokio = { version = "1", features = ["full"] }
log = "0.4"
env_logger = "0.11"